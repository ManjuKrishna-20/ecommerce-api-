name: Ecommerce Multi-Environment Deployment

on:
  push:
    branches:
      - dev
      - qa
      - production

  workflow_dispatch:
    inputs:
      environment:
        description: "Select deployment environment"
        required: true
        type: choice
        options:
          - dev
          - qa
          - prod

permissions:
  contents: read

env:
  AWS_REGION: ap-south-1
  ECS_CLUSTER: ecommerce-cluster
  CONTAINER_NAME: ecommerce-api

jobs:
  deploy:

    runs-on: ubuntu-latest

    # ============================================================
    # Select GitHub Environment
    # ============================================================
    environment: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.environment || (github.ref_name == 'production' && 'prod' || github.ref_name) }}

    steps:

      # ============================================================
      # 1. Checkout Source Code
      # ============================================================
      - name: Checkout Repository
        uses: actions/checkout@v4


      # ============================================================
      # 2. Configure AWS Credentials
      # ============================================================
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}


      # ============================================================
      # 3. Login to Amazon ECR
      # ============================================================
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2


      # ============================================================
      # 4. Select Deployment Environment
      # ============================================================
      - name: Select Environment
        shell: bash
        run: |

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then

            ENVIRONMENT="${{ github.event.inputs.environment }}"

          else

            BRANCH="${GITHUB_REF_NAME}"

            if [ "$BRANCH" = "dev" ]; then
              ENVIRONMENT="dev"

            elif [ "$BRANCH" = "qa" ]; then
              ENVIRONMENT="qa"

            elif [ "$BRANCH" = "production" ]; then
              ENVIRONMENT="prod"

            else
              echo "Invalid branch: $BRANCH"
              exit 1
            fi

          fi

          echo "DEPLOY_ENV=$ENVIRONMENT" >> "$GITHUB_ENV"

          echo "=============================================="
          echo "SELECTED ENVIRONMENT"
          echo "=============================================="
          echo "Environment : $ENVIRONMENT"
          echo "Branch      : $GITHUB_REF_NAME"
          echo "Event       : ${{ github.event_name }}"
          echo "=============================================="


      # ============================================================
      # 5. Select ECR Repository / ECS Service / Task Definition
      # ============================================================
      - name: Select Deployment Resources
        shell: bash
        run: |

          if [ "$DEPLOY_ENV" = "dev" ]; then

            echo "ECR_REPOSITORY=ecommerce-api-dev" >> "$GITHUB_ENV"
            echo "ECS_SERVICE=ecommerce-dev-service" >> "$GITHUB_ENV"
            echo "TASK_DEFINITION=.github/workflows/task-definition-dev.json" >> "$GITHUB_ENV"

          elif [ "$DEPLOY_ENV" = "qa" ]; then

            echo "ECR_REPOSITORY=ecommerce-api-qa" >> "$GITHUB_ENV"
            echo "ECS_SERVICE=ecommerce-qa-service" >> "$GITHUB_ENV"
            echo "TASK_DEFINITION=.github/workflows/task-definition-qa.json" >> "$GITHUB_ENV"

          elif [ "$DEPLOY_ENV" = "prod" ]; then

            echo "ECR_REPOSITORY=ecommerce-api-prod" >> "$GITHUB_ENV"
            echo "ECS_SERVICE=ecommerce-prod-service" >> "$GITHUB_ENV"
            echo "TASK_DEFINITION=.github/workflows/task-definition-prod.json" >> "$GITHUB_ENV"

          else

            echo "Invalid deployment environment"
            exit 1

          fi

          echo "=============================================="
          echo "DEPLOYMENT RESOURCES"
          echo "=============================================="
          echo "Environment     : $DEPLOY_ENV"
          echo "ECR Repository  : $ECR_REPOSITORY"
          echo "ECS Service     : $ECS_SERVICE"
          echo "Task Definition : $TASK_DEFINITION"
          echo "=============================================="


      # ============================================================
      # 6. Generate Docker Image Tag
      #
      # Format:
      #
      # dev-20260811-1359-b22
      # qa-20260811-1222-b19
      # prod-20260811-1430-b25
      #
      # ============================================================
      - name: Generate Image Tag
        shell: bash
        run: |

          BUILD_NUMBER="${GITHUB_RUN_NUMBER}"

          DATE=$(date -u +"%Y%m%d")
          TIME=$(date -u +"%H%M")

          IMAGE_TAG="${DEPLOY_ENV}-${DATE}-${TIME}-b${BUILD_NUMBER}"

          echo "BUILD_NUMBER=$BUILD_NUMBER" >> "$GITHUB_ENV"
          echo "BUILD_DATE=$DATE" >> "$GITHUB_ENV"
          echo "BUILD_TIME=$TIME" >> "$GITHUB_ENV"
          echo "IMAGE_TAG=$IMAGE_TAG" >> "$GITHUB_ENV"

          echo "=============================================="
          echo "DOCKER IMAGE TAG"
          echo "=============================================="
          echo "Environment : $DEPLOY_ENV"
          echo "Date        : $DATE"
          echo "Time        : $TIME"
          echo "Build       : $BUILD_NUMBER"
          echo "Image Tag   : $IMAGE_TAG"
          echo "=============================================="


      # ============================================================
      # 7. Build Docker Image
      # ============================================================
      - name: Build Docker Image
        shell: bash
        run: |

          echo "=============================================="
          echo "BUILDING DOCKER IMAGE"
          echo "=============================================="

          docker build \
            -t "$ECR_REPOSITORY:$IMAGE_TAG" .

          echo "Docker image built successfully."

          echo "=============================================="
          echo "Repository : $ECR_REPOSITORY"
          echo "Image Tag  : $IMAGE_TAG"
          echo "=============================================="


      # ============================================================
      # 8. Tag Docker Image for ECR
      # ============================================================
      - name: Tag Docker Image
        shell: bash
        run: |

          ECR_REGISTRY="${{ steps.login-ecr.outputs.registry }}"

          docker tag \
            "$ECR_REPOSITORY:$IMAGE_TAG" \
            "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

          echo "=============================================="
          echo "DOCKER IMAGE TAGGED"
          echo "=============================================="
          echo "Image URI:"
          echo "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
          echo "=============================================="


      # ============================================================
      # 9. Push Docker Image to ECR
      # ============================================================
      - name: Push Docker Image
        shell: bash
        run: |

          ECR_REGISTRY="${{ steps.login-ecr.outputs.registry }}"

          IMAGE_URI="$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

          echo "=============================================="
          echo "PUSHING DOCKER IMAGE"
          echo "=============================================="
          echo "Repository : $ECR_REPOSITORY"
          echo "Image Tag  : $IMAGE_TAG"
          echo "Image URI  : $IMAGE_URI"
          echo "=============================================="

          docker push "$IMAGE_URI"

          echo "Docker image pushed successfully."


      # ============================================================
      # 10. Show Current Image Information
      # ============================================================
      - name: Show Image Information
        shell: bash
        run: |

          ECR_REGISTRY="${{ steps.login-ecr.outputs.registry }}"

          IMAGE_URI="$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

          echo "=============================================="
          echo "DEPLOYMENT IMAGE INFORMATION"
          echo "=============================================="
          echo "Environment : $DEPLOY_ENV"
          echo "Repository  : $ECR_REPOSITORY"
          echo "Image Tag   : $IMAGE_TAG"
          echo "Build       : $BUILD_NUMBER"
          echo "Date        : $BUILD_DATE"
          echo "Time        : $BUILD_TIME"
          echo "Image URI   : $IMAGE_URI"
          echo "=============================================="


      # ============================================================
      # 11. Show Latest 5 NEW-FORMAT Builds
      #
      # IMPORTANT:
      #
      # This ignores old images such as:
      #
      # v1.0.5-dev-b5
      # v1.0.20-dev-b20
      #
      # and only displays:
      #
      # dev-YYYYMMDD-HHMM-bBUILD
      #
      # ============================================================
      - name: Show Latest 5 Builds
        shell: bash
        run: |

          echo "=============================================="
          echo "LATEST 5 BUILDS"
          echo "=============================================="
          echo "Environment : $DEPLOY_ENV"
          echo "Repository  : $ECR_REPOSITORY"
          echo "=============================================="

          aws ecr describe-images \
            --repository-name "$ECR_REPOSITORY" \
            --region "$AWS_REGION" \
            --output json > ecr-images.json

          jq -r \
            --arg ENV "$DEPLOY_ENV" '
              [
                .imageDetails[]
                | select(.imageTags != null)
                | . as $image
                | $image.imageTags[]
                | select(startswith($ENV + "-"))
                | {
                    tag: .,
                    pushedAt: $image.imagePushedAt
                  }
              ]
              | sort_by(.pushedAt)
              | reverse
              | .[:5]
              | .[]
              | [.tag, .pushedAt]
              | @tsv
            ' ecr-images.json \
            | awk 'BEGIN {
                printf "%-35s %-30s\n", "IMAGE TAG", "PUSHED AT"
                printf "%-35s %-30s\n", "-----------------------------------", "------------------------------"
              }
              {
                printf "%-35s %-30s\n", $1, $2
              }'

          echo "=============================================="
          echo "Latest 5 builds displayed successfully."
          echo "=============================================="


      # ============================================================
      # 12. Render ECS Task Definition
      # ============================================================
      - name: Render ECS Task Definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: ${{ env.TASK_DEFINITION }}
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}


      # ============================================================
      # 13. Deploy to Amazon ECS
      # ============================================================
      - name: Deploy to Amazon ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true


      # ============================================================
      # 14. Verify ECS Deployment
      # ============================================================
      - name: Verify ECS Deployment
        shell: bash
        run: |

          echo "=============================================="
          echo "ECS DEPLOYMENT VERIFICATION"
          echo "=============================================="

          aws ecs describe-services \
            --cluster "$ECS_CLUSTER" \
            --services "$ECS_SERVICE" \
            --region "$AWS_REGION" \
            --query 'services[0].{
              Service:serviceName,
              Status:status,
              Running:runningCount,
              Desired:desiredCount,
              TaskDefinition:taskDefinition
            }' \
            --output table

          echo "=============================================="
          echo "DEPLOYMENT COMPLETED SUCCESSFULLY"
          echo "=============================================="
          echo "Environment : $DEPLOY_ENV"
          echo "Image       : $IMAGE_TAG"
          echo "Service     : $ECS_SERVICE"
          echo "=============================================="